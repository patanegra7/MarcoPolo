<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="cat.dme.smart.marcopolo">

    <application
        android:name=".application.MarcoPoloApplication"
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/MarcoPoloTheme">
        <activity android:name=".activity.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    <!--activity
android:name=".activities.DailyActivity"
android:launchMode="singleTask" />
<activity
android:name=".activities.SummaryActivity"
android:launchMode="singleTask" />
<activity
android:name=".activities.ExportActivity"
android:launchMode="singleTask" / -->
        <activity android:name=".activity.CreditsActivity" android:launchMode="singleTask" />
        <activity android:name=".activity.SettingsActivity" android:launchMode="singleTask" />
        <activity android:name=".activity.TripActivity" android:launchMode="singleTask" />
        <activity android:name=".activity.EditTripActivity" android:launchMode="singleTask" />
<!--activity
android:name=".activities.EditConceptActivity"
android:launchMode="singleTask" />
<activity
android:name=".activities.EditCurrencyActivity"
android:launchMode="singleTask" />
<activity
android:name=".activities.EditPayerActivity"
android:launchMode="singleTask" />
<activity
android:name=".activities.EditPaymentMethodActivity"
android:launchMode="singleTask" />
<activity
android:name=".activities.EditExpenseActivity"
android:launchMode="singleTask" /-->
    </application>
<!--android:roundIcon="@mipmap/ic_launcher_round"-->
<!--
  <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
-->
</manifest>
