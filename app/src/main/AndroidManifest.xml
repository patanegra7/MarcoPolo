<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="cat.dme.smart.marcopolo">

    <!-- android:name=".legacy.model.global.MarcoPoloApplication" -->

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
    <!--android:roundIcon="@mipmap/ic_launcher_round"-->
<!--
  <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:name=".model.global.MarcoPoloApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/MarcoPoloTheme">

        <activity android:name=".activities.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activities.DailyActivity"
            android:launchMode="singleTask" />
        <activity
            android:name=".activities.SummaryActivity"
            android:launchMode="singleTask" />
        <activity
            android:name=".activities.ExportActivity"
            android:launchMode="singleTask" />
        <activity
            android:name=".activities.CreditsActivity"
            android:launchMode="singleTask" />
        <activity
            android:name=".activities.SettingsActivity"
            android:launchMode="singleTask" />
        <activity
            android:name=".activities.TripActivity"
            android:launchMode="singleTask" />
        <activity
            android:name=".activities.EditTripActivity"
            android:launchMode="singleTask" />
        <activity
            android:name=".activities.EditConceptActivity"
            android:launchMode="singleTask" />
        <activity
            android:name=".activities.EditCurrencyActivity"
            android:launchMode="singleTask" />
        <activity
            android:name=".activities.EditPayerActivity"
            android:launchMode="singleTask" />
        <activity
            android:name=".activities.EditPaymentMethodActivity"
            android:launchMode="singleTask" />
        <activity
            android:name=".activities.EditExpenseActivity"
            android:launchMode="singleTask" />
    </application>
-->

</manifest>
